import React from 'react';
import { Container } from '../components'

const WIP = ({name}) => {

    return(
        <Container>
            <div>
                `This is ${name} view. Work still in progress`  
            </div>
        </Container>
    )
}

export default WIP;